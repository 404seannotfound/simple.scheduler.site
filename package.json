{
  "name": "simple-anonymous-scheduler",
  "version": "1.0.0",
  "private": true,
  "description": "SimpleAnonymousScheduler monorepo",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix client\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix client\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
